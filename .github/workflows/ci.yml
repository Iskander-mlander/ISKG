name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - run: pip install ruff
      - run: ruff check iskg/

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - run: pip install pyright pywebview
      - run: pip install -e .
      - run: pyright iskg/

  test-linux:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install system deps (pywebview)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-gi python3-gi-cairo gir1.2-gtk-3.0
          apt-cache show gir1.2-webkit2-4.1 >/dev/null 2>&1 && \
            sudo apt-get install -y -qq gir1.2-webkit2-4.1 || \
            sudo apt-get install -y -qq gir1.2-webkit2-4.0
      - run: pip install -e ".[dev]"
      - run: pytest

  test-win:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, windows-2025]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: pytest

  test-mac:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-13, macos-14, macos-15]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev]"
      - run: pytest

  test-distros:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        container:
          - "fedora:41"
          - "debian:12"
          - "archlinux:latest"
    container:
      image: ${{ matrix.container }}
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps
        id: distro
        run: |
          case "${{ matrix.container }}" in
            fedora*)
              dnf install -y python3 python3-pip python3-devel git
              ;;
            debian*)
              apt-get update -qq
              DEBIAN_FRONTEND=noninteractive apt-get install -y -qq python3 python3-pip python3-venv python3-dev git
              ;;
            archlinux*)
              pacman -Syu --noconfirm python python-pip git
              ;;
          esac
      - name: Debug info
        run: |
          python3 --version
          python3 -m pip --version
      - name: Install project
        run: |
          python3 -m pip install --break-system-packages -e ".[dev]"
      - name: Run tests
        run: |
          python3 -m pytest -v
